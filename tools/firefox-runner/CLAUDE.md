Read README.md in this directory before changing anything here or debugging a
Firefox launch. It is one problem -- Firefox does not start on this machine --
and the README carries the six hypotheses already eliminated, so a fresh look
at the same failure costs a re-read rather than an afternoon.

`PRCODER_BROWSER=chromium` is the way past it for any work that is not about
Firefox itself.
